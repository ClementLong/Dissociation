{% include '/partials/header.twig' %}

<div class="container home">
    <h1><img src="{{ app.request.basepath }}/image/DISSOCIATION_home.png" alt="Dissociation" style="width: 100%;"></h1>
    <video
            src="media/intro.mp4"
            autoplay="autoplay"
            loop = "loop"
    ></video>
    <a href="{{ url('intro') }}">{{ data.ButtonHeader }}</a>
</div>

{% include('/partials/footer.twig') %}